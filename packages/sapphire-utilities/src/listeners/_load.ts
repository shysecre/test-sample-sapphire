// import { container } from "@sapphire/pieces"

// TODO: Maybe readdir, get files, and register in a loop?

export function loadListeners() {
  // const store = "listeners" as const
  // void container.stores.loadPiece({
  //   name: ChatInputCommandDenied.name,
  //   piece: ChatInputCommandDenied,
  //   store,
  // })
  // void container.stores.loadPiece({
  //   name: ChatInputCommandError.name,
  //   piece: ChatInputCommandError,
  //   store,
  // })
  // void container.stores.loadPiece({
  //   name: ChatInputCommandSuccess.name,
  //   piece: ChatInputCommandSuccess,
  //   store,
  // })
  // void container.stores.loadPiece({
  //   name: ChatInputSubcommandError.name,
  //   piece: ChatInputSubcommandError,
  //   store,
  // })
  // void container.stores.loadPiece({
  //   name: InteractionHandlerErrorEvent.name,
  //   piece: InteractionHandlerErrorEvent,
  //   store,
  // })
}
