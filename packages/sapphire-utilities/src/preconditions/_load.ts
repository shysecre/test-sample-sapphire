// import { container } from "@sapphire/pieces"
export function loadPreconditions() {
  // const store = "preconditions" as const
  // void container.stores.loadPiece({ name: EventModOnly.name, piece: EventModOnly, store })
  // void container.stores.loadPiece({ name: StaffTypeOnly.name, piece: StaffTypeOnly, store })
  // void container.stores.loadPiece({ name: OwnerOnly.name, piece: OwnerOnly, store })
  // void container.stores.loadPiece({
  //   name: EventModOrCloseMakerOnly.name,
  //   piece: EventModOrCloseMakerOnly,
  //   store,
  // })
}
