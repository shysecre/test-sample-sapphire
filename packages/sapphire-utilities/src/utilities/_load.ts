// import { container } from "@sapphire/pieces"

// TODO: readdir, get files, register in a loop?

export function loadUtilities() {
  // const store = "utilities" as const
  // void container.stores.loadPiece({ name: EconomyUtility.name, piece: EconomyUtility, store })
  // void container.stores.loadPiece({ name: CacheUtility.name, piece: CacheUtility, store })
  // void container.stores.loadPiece({ name: UserUtility.name, piece: UserUtility, store })
  // void container.stores.loadPiece({ name: InventoryUtility.name, piece: InventoryUtility, store })
  // void container.stores.loadPiece({ name: LoveRoomUtility.name, piece: LoveRoomUtility, store })
  // void container.stores.loadPiece({ name: HolidayUtility.name, piece: HolidayUtility, store })
  // void container.stores.loadPiece({ name: WarnUtility.name, piece: WarnUtility, store })
  // void container.stores.loadPiece({
  //   name: TransactionUtility.name,
  //   piece: TransactionUtility,
  //   store,
  // })
  // void container.stores.loadPiece({
  //   name: PersonalRoleUtility.name,
  //   piece: PersonalRoleUtility,
  //   store,
  // })
}
